import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { Map } from './Map';

// Representa la aplicación, por ahora únicamente el mapa
ReactDOM.render(<Map horizontal="9" vertical="12" />, document.getElementById("root"));
